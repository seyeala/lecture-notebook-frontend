name: Deploy documentation (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment intentionally disabled
        run: |
          echo "The upstream Butterfly deploy workflow is disabled for this fork."
          echo "Do not re-enable deployment until the Lecture Notebook preview/release pipeline is designed, reviewed, and documented."
          echo "See project-docs/08-decisions/ADR-0004-disable-upstream-release-pipeline.md."
